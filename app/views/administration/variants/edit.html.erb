<h1>Editing variant</h1>

<%= render 'form', action_url: administration_variant_path(@variant) %>

<%= link_to 'Show', administration_variant_path(@variant) %> |
<%= link_to 'Back', administration_variants_path %>
